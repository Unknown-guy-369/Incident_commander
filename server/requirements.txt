# Runtime-only dependencies for the HF Space Docker image.
# Keep this file tiny: every line here gets baked into the deployed image.
openenv-core[core]>=0.2.2
fastapi>=0.115.0
uvicorn[standard]>=0.24.0
pydantic>=2.7.0
gradio>=4.44.0
